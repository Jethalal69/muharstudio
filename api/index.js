/**
 * MUHAR STUDIO — Vercel Serverless Function Entry Point
 * Routes all API & Admin requests through the centralized Express application.
 */

const app = require('../server');

module.exports = app;
